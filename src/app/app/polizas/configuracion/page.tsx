import { ConfiguracionView } from './configuracion-view'

// Bloque UI-0 (prototipo visual): catálogos mock en memoria, independientes
// de los catálogos reales creados en Admin (Bloque Técnico 1, aún sin migrar).
export default function ConfiguracionPage() {
  return <ConfiguracionView />
}
